# 완벽한 2-Pane Vibe(BibTeX) 관리자 및 서식 주입 안정화 패치 완료!

이번 패치에서는 사용자가 참조 파일을 좀 더 직관적이고 완벽하게 제어할 수 있도록 **참조 파일(Reference) 관리 도구의 전면 개편**과 함께, 기존에 존재했던 **저장 시점 레이스 컨디션 버그를 완벽하게 수정**하였습니다.

## 주요 변경 사항 요약

### 1. `ReferenceManagerModal` 2-Pane 구조로 전면 개편
- 기존에는 파일 이름과 내용을 한 번 입력해서 단순히 새로 만들기만 가능했던 모달을 **좌측 목록(파일 리스트)과 우측 뷰(에디터)** 형식의 2-Pane UI로 완벽하게 뜯어고쳤습니다.
- **조회**: 모달을 열면 리소스 폴더에 있는 모든 `.bib` 파일 목록을 스캔하여 좌측 사이드바에 표시합니다.
- **선택 및 편집**: 리스트에서 파일을 클릭하면, 우측 화면에 그 파일의 내용이 불러와지며 즉시 수정 후 저장(덮어쓰기)할 수 있습니다.
- **새로 만들기**: 좌측 최상단의 `+ 새 파일 만들기` 버튼을 눌러 언제든지 비어있는 새로운 참조 문서를 생성할 수 있습니다.
- **삭제**: 리스트의 항목에 마우스를 올리면 `휴지통(🗑️)` 아이콘이 나타나며, 클릭 시 확인 알림창을 띄우고 즉시 해당 파일을 삭제할 수 있습니다.

### 2. 저장 기능(Ctrl+S / 자동 저장) 상태 버그 완벽 수정
- "저장 시 활성화된 서식(CSS)을 Frontmatter에 강제 주입하는 기능"이 동작한 직후에 에디터 탭에 **"수정됨(노란 점)"** 표시가 지워지지 않고 남아있던 현상을 수정했습니다.
- **해결**: 모든 수동 저장 로직에서 0.15초의 딜레이를 주고 강제로 `isModified = false` 상태를 덮어씌움으로써, 서식 강제 주입이 성공적으로 작동하면서도 화면 상으로는 저장이 완료된 상태를 유지하도록 안정화시켰습니다.

### 3. 에디터 하단 및 마지막 줄 입력 시 미리보기 스크롤 요동/가려짐 현상 수정 (2026-08-12 추가)
- **현상**: 에디터 마지막 10줄 이내를 타이핑할 때 에디터의 스크롤 위치 변화로 인해 미리보기 스크롤이 매번 위로 틀어져 올라가 화면 끝이 가려지는 버그가 발생했습니다.
- **해결**:
  - 기존의 불안정한 실시간 Y축 비율 동기화 리스너를 완전히 걷어냈습니다.
  - 에디터 스크롤 변경 이벤트(`onDidScrollChange`) 내부에 **하단 밀착 스크롤 가드**를 적용하여, 에디터의 끝 라인 주변이 보일 때는 미리보기 스크롤을 맨 아래로 즉시 안착시켜 올라가지 못하도록 제어했습니다.
  - 커서 이동 이벤트(`onDidChangeCursorPosition`) 내에서도 마지막 2줄 이내에서 줄바꿈이나 텍스트를 쓸 때 미리보기를 하단에 바로 고정시키도록 통합 보정했습니다.
  - 이 조치로 타이핑 시 미리보기가 춤을 추거나 마지막 줄이 아래로 가려지는 현상이 물리적으로 완벽 차단되었습니다.

---

이제 **상단 툴바의 책 아이콘(📚)**을 눌러 새롭게 탈바꿈한 참조 파일 관리자를 자유롭게 테스트해 보세요!
